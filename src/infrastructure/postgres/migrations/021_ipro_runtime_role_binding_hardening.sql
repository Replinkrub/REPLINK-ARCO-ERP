-- RETIRED: exceptional orphan recovery ownership hardening is retired because
-- normal reimport was proven business-equivalent. This records retirement only:
-- it changes no ownership, roles, memberships, ACLs, runtime contract,
-- commercial data, or orphan state.
SELECT 1;
